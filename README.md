# hashicorp-packages

A Docker-based project that configures the official [HashiCorp package repositories](https://www.hashicorp.com/en/official-packaging-guide) for multiple Linux platforms and lists the available HashiCorp packages on each.

## Platforms

| Service        | Base image      | Package manager | HashiCorp repo |
|----------------|-----------------|-----------------|----------------|
| `rhel8`        | AlmaLinux 8     | yum             | RPM (`RHEL/8`) |
| `rhel9`        | AlmaLinux 9     | dnf             | RPM (`RHEL/9`) |
| `rhel10`       | AlmaLinux 9 userspace | dnf      | RPM (`RHEL/10`) |
| `ubuntu-noble` | Ubuntu 24.04 LTS | apt            | APT (`noble`)  |
| `ubuntu-jammy` | Ubuntu 22.04 LTS | apt            | APT (`jammy`)  |

## Project layout

```
.
├── docker-compose.yml          # Orchestrates all 5 platform containers
├── docker/
│   ├── rhel8/Dockerfile        # RHEL 8 compatible (AlmaLinux 8)
│   ├── rhel9/Dockerfile        # RHEL 9 compatible (AlmaLinux 9)
│   ├── rhel10/Dockerfile       # RHEL 10 repo query from EL9 userspace
│   ├── ubuntu-noble/Dockerfile # Ubuntu 24.04 LTS
│   └── ubuntu-jammy/Dockerfile # Ubuntu 22.04 LTS
└── output/                     # Generated by docker compose (gitignored)
    ├── hashicorp-packages-rhel8.txt
    ├── hashicorp-packages-rhel9.txt
    ├── hashicorp-packages-rhel10.txt
    ├── hashicorp-packages-ubuntu-noble.txt
    └── hashicorp-packages-ubuntu-jammy.txt
```

## Usage

### List available HashiCorp packages on all platforms

```bash
docker compose up --build
```

Each service builds its image (configuring the HashiCorp repo during the build), then prints the list of available HashiCorp packages for that platform **and writes a labelled output file** to the `./output/` directory on the host.

### Output files

After running, the `./output/` directory will contain one tab-separated file per platform listing the latest available version of each HashiCorp package:

```
output/
  hashicorp-packages-rhel8.txt
  hashicorp-packages-rhel9.txt
  hashicorp-packages-rhel10.txt
  hashicorp-packages-ubuntu-noble.txt
  hashicorp-packages-ubuntu-jammy.txt
```

Each file has the format `<package-name>\t<latest-version>`, for example:

```
boundary.x86_64    0.19.3-1
consul.x86_64      1.21.1-1
terraform.x86_64   1.12.0-1
vault.x86_64       1.19.2-1
```

### Run a single platform

```bash
# RHEL 8
docker compose run --rm rhel8

# RHEL 9
docker compose run --rm rhel9

# RHEL 10
docker compose run --rm rhel10

# Ubuntu 24.04 LTS (Noble)
docker compose run --rm ubuntu-noble

# Ubuntu 22.04 LTS (Jammy)
docker compose run --rm ubuntu-jammy
```

### Build images without running

```bash
docker compose build
```

## How it works

Each Dockerfile:

1. **Starts from** the official base image for the target platform.
2. **Adds the HashiCorp package repository** using the method documented in the [official HashiCorp packaging guide](https://www.hashicorp.com/en/official-packaging-guide):
   - **RHEL-based**: installs `yum-utils` / `dnf-plugins-core`, configures the HashiCorp RPM repo, and refreshes the metadata cache. The `rhel10` image pins its repo config directly to `RHEL/10/x86_64/stable` while running on an EL9 userspace so it still works on hosts that cannot run current EL10 base images.
   - **Ubuntu-based**: installs the HashiCorp GPG key, adds the HashiCorp APT source, refreshes the package lists, and decompresses the HashiCorp `.lz4` package index when generating the output.
3. **Default command** lists the **latest available version** of each HashiCorp package (as `name\tversion`), prints it to stdout, and simultaneously writes it to a labelled file under `/output/` (e.g. `hashicorp-packages-rhel9.txt`). The `./output/` directory is mounted from the host via docker-compose so the files are accessible after the container exits.
